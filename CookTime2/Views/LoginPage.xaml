<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CookTime2.Views.LoginPage">
    <ContentPage.Content>
        <ScrollView>
        <StackLayout>
                <Grid Margin="20,0,20,0" VerticalOptions="CenterAndExpand">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Entry Placeholder="Email" FontSize="16" x:Name="EntryEmail" Grid.Row="0"/>
                <Entry Placeholder="Password" IsPassword="True" FontSize="16" x:Name="EntryPassword" Grid.Row="1"/>
                <Button Text="Log In" TextColor="White" BackgroundColor="Blue" Clicked="Button_Clicked" Grid.Row="2"/>
                <Button Text="Sing Up" Grid.Row="3" Clicked="Button_Clicked_1"/>
                </Grid>
        </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>